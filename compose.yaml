name: chollometro-alerts

x-app: &app
  image: chollometro-alerts:runtime
  build: .
  env_file:
    - path: .env
      required: false
  environment:
    DATABASE_PATH: /app/data/chollometro.sqlite3
  restart: unless-stopped
  volumes:
    - chollometro-data:/app/data

services:
  telegram:
    <<: *app
    command: ["chollometro-alerts", "telegram-listen"]

  scanner:
    <<: *app
    command: ["chollometro-alerts", "scan"]
    healthcheck:
      test: ["CMD", "chollometro-alerts", "health"]
      interval: 60s
      timeout: 10s
      start_period: 60s
      retries: 3

volumes:
  chollometro-data:
