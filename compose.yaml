services:
  chollometro-alerts:
    build: .
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_PATH: /app/data/chollometro.sqlite3
    restart: unless-stopped
    volumes:
      - ./data:/app/data
