services:
  chollometro-alerts:
    build: .
    command: ["chollometro-alerts", "run"]
    env_file:
      - path: .env
        required: false
    environment:
      DATABASE_PATH: ${DATABASE_PATH:-/app/data/chollometro.sqlite3}
    restart: unless-stopped
    volumes:
      - ./data:/app/data
